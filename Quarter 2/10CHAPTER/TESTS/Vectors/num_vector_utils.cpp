#include <string>
#include <vector>
using namespace std;

string render_num_vector(const vector<int>&) {
    vector <int> x;
    string a = x[0];
    string b = x[1];
    string c = x[2];
    string s = s.push_back(c).pushback(b).pushback(a);
    return s;
}