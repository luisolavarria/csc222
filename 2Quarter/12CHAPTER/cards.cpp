#include <iostream>
#include "cards.h"
#include <vector>
#include <string>

using namespace std;

int main () {
    Card card(2, 11);
cout << card.to_string() << endl;
    Card to_string(string a, string b);
}
